# Postgres connection for Prisma
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sitdb?schema=public"

# JWT
JWT_SECRET="supersecretjwt"

# CORS frontend origin
CLIENT_ORIGIN="http://localhost:3000"

# Cloudflare R2 (S3-compatible) credentials
R2_ACCOUNT_ID="xxxxxx"
R2_ACCESS_KEY_ID="AKIA..."
R2_SECRET_ACCESS_KEY="..."
R2_BUCKET_NAME="sit-transcripts"
R2_PUBLIC_BASE_URL="https://<custom-domain-or-public-bucket-host>/"
# If using private bucket, leave PUBLIC_BASE_URL empty, we will generate signed URLs.
